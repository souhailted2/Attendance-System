name: نشر تلقائي على Hostinger

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: تشغيل النشر عبر deploy.php
        run: |
          echo "جارٍ استدعاء سكريبت النشر على السيرفر..."
          curl -f --max-time 60 "https://allal.alllal.com/deploy.php?token=12300"
          echo "تم النشر بنجاح ✓"
