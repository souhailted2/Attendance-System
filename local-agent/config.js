/**
 * إعدادات الوكيل المحلي
 * عدّل هذه القيم حسب بيئتك
 */
module.exports = {
  // ── إعدادات جهاز ZKTeco ──────────────────────────────
  device: {
    ip: "192.168.1.99",      // عنوان IP للجهاز
    port: 4370,              // المنفذ (الافتراضي 4370)
    timeout: 10000,          // مهلة الاتصال بالثواني
    serialNumber: "CKPG231760261", // الرقم التسلسلي للجهاز
  },

  // ── إعدادات السيرفر ───────────────────────────────────
  server: {
    url: "http://allal.alllal.com/api/sync/local-agent",
    // ضع هنا API key إذا أضفت حماية للـ endpoint
    apiKey: "",
  },

  // ── إعدادات المزامنة ──────────────────────────────────
  sync: {
    intervalMinutes: 10,     // كل كم دقيقة يتم السحب
    daysBack: 1,             // كم يوم للخلف يتم سحبه (1 = اليوم فقط)
  },
};
